import { RecoveryScreen } from "@/components/system/RecoveryScreen"

export default function NotFound() {
  return (
    <RecoveryScreen
      title="Page not found"
      description="We couldn't find that page. It may have been removed or the link might be incorrect."
    />
  )
}
